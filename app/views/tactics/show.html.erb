<p id="notice"><%= notice %></p>
<script type="text/javascript">
$("#toto").click(function(){
	alert('coucou');
});
</script>

<p>
	<strong>Name:</strong>
	<%= @tactic.name %>

</p>

<p>
	<%= form_tag "/sequences", method: "post" do %>
	<%= link_to 'New Sequence', new_tactic_sequence_path(@tactic) %>
	<!--<input type="button" id="toto" value="Ajouter une séquence" />-->
	<% end %>
</p>


<p>
	<strong>Séquence:</strong><br>
  	<% @tactic.sequences.each do |sequence| %>
		<tr>
 			<td><%= sequence.name %></td>
		</tr>
	<% end %>

</p>

<p>
	<strong>Markers:</strong><br>
  	<% @tactic.sequences.first.markers.each do |marker| %>
		<tr>
 			<td><%= marker.name %></td><br>
		</tr>
	<% end %>

</p>



<%= image_tag 'terrain.jpg' %>


<%= link_to 'Edit', edit_tactic_path(@tactic) %> |
<%= link_to 'Back', tactics_path %>
